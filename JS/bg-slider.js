(function () {
  const swiper = new Swiper(".top-wrapper__swiper", {
    // Optional parameters
    direction: "horizontal",
    loop: true,
    effect: "fade",
    speed: 10000,
    autoplay: {
      delay: 2000,
    },
  });
})();
